# .github/workflows/android_build.yml

name: Android Build with Buildozer

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Setup Java (for Android tools)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    # 1. Autoconf, Libtool 등 필수 네이티브 빌드 도구 설치
    - name: Install system and native build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends \
          build-essential git wget curl unzip ca-certificates \
          autoconf automake libtool libtool-bin m4 \
          pkg-config gettext \
          libffi-dev libssl-dev zlib1g-dev \
          python3-dev

    # 2. Autoconf 오류 최종 해결 단계
    - name: Conditional autoupdate/autoreconf
      run: |
        if [ -f configure.ac ]; then
          autoupdate || true
          autoreconf -fi || true
        fi

    - name: Upgrade pip and install Python build tools
      run: |
        python -m pip install --upgrade pip setuptools wheel
        pip install --upgrade Cython buildozer python-for-android

    # =========================================================
    # 🛑 [핵심 해결] SDK 경로/Aidl/License 오류를 위한 수동 SDK 설정
    # =========================================================
    - name: Install Android SDK command-line tools and build-tools
      env:
        ANDROID_SDK_ROOT: /home/runner/.buildozer/android/platform/android-sdk
      run: |
        set -e
        sdk_root="$ANDROID_SDK_ROOT"
        mkdir -p "$sdk_root"

        # 1) Command Line Tools 다운로드 및 설치 (최신 버전 사용)
        wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O /tmp/cmdline-tools.zip
        unzip -q /tmp/cmdline-tools.zip -d "$sdk_root/cmdline-tools"

        # 압축 해제된 디렉터리 이름 정규화
        if [ -d "$sdk_root/cmdline-tools/cmdline-tools" ]; then
          mv "$sdk_root/cmdline-tools/cmdline-tools" "$sdk_root/cmdline-tools/latest"
        fi
        
        # 2) Buildozer가 기대하는 레거시 경로(tools/bin)에 심볼릭 링크 생성
        mkdir -p "$sdk_root/tools/bin"
        ln -sf "$sdk_root/cmdline-tools/latest/bin/sdkmanager" "$sdk_root/tools/bin/sdkmanager"
        chmod +x "$sdk_root/tools/bin/sdkmanager"
        
        # 3) PATH 설정 (sdkmanager, platform-tools 경로 추가)
        export PATH="$sdk_root/cmdline-tools/latest/bin:$sdk_root/tools/bin:$PATH"

        # 4) 라이선스 수락 및 필수 패키지 설치
        yes | sdkmanager --sdk_root="$sdk_root" --licenses || true
        sdkmanager --sdk_root="$sdk_root" \
          "platform-tools" \
          "platforms;android-33" \
          "build-tools;33.0.2"

    # 모든 환경 설정이 완료된 후 Buildozer 실행 (환경 변수 재설정 필수)
    - name: Run Buildozer (Final Build)
      run: buildozer android debug --verbose
      env:
        BUILD_ACCEPT_SDK_LICENSE: 1
        ANDROID_SDK_ROOT: /home/runner/.buildozer/android/platform/android-sdk
        ANDROID_HOME: /home/runner/.buildozer/android/platform/android-sdk
        # 이전 스텝에서 설정된 PATH를 포함하여 모든 경로를 명시적으로 전달
        PATH: /home/runner/.buildozer/android/platform/android-sdk/tools/bin:/home/runner/.buildozer/android/platform/android-sdk/platform-tools:$PATH


    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: english-word-quiz-apk
        path: bin/*.apk